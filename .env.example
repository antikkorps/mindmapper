# Database Configuration
# Option 1: Cloud Database (Neon, Supabase, etc.) - Recommended for production
# Uncomment and use DATABASE_URL for cloud databases
# DATABASE_URL=postgres://user:password@host:5432/database?sslmode=require

# Option 2: Local Docker PostgreSQL - For development
DB_HOST=localhost
DB_PORT=5140
DB_USER=mindmapper
DB_PASSWORD=mindmapper_dev
DB_NAME=mindmapper_db
DB_DIALECT=postgres
DB_SSL=false

# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Configuration (for future auth)
# openssl rand -base64 32
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# CORS Configuration
CORS_ORIGIN=http://localhost:5173
